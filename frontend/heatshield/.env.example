# Leave blank for local development with the Vite /api proxy.
# In production, set this to the deployed FastAPI origin, for example:
# VITE_API_BASE_URL=https://api.your-domain.com
VITE_API_BASE_URL=
